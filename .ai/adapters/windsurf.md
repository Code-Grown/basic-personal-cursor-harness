# Adapter: Windsurf (Cascades)

## Wiring
1. Prioriza `AGENTS.md` + `.cursor/rules/` si el producto las indexa.
2. Si hay “Workspace Rules” / memories del IDE: pegar resumen de `session-gate` + confidentiality (≤40 líneas), no el repo entero.
3. Cascade: pedir explícitamente “no editar hasta confirmar A/B” en tareas no triviales.

## Tip
Windsurf a veces indexa mucho: usa `@` selectivo (constitution + gate) en vez de carpeta `.ai` completa.
