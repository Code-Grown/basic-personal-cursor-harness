# Adapter: Claude (Projects / Claude Code)

## Wiring
1. `CLAUDE.md` en raíz (Claude Code lo carga).
2. Claude Projects: pegar o subir `constitution` + `session-gate` + `operating-mode.compact` (+ ultra si Haiku).
3. Actualizar `memory/` tras sesiones relevantes.

## Carga mínima sugerida
`CLAUDE.md` → constitution → session-gate → confidentiality → compact mode
